/**
 * Client renderer for the stores page used by realtime updates.
 */
'use client';

import StoresContentClient from './StoresContentClient';

// Re-export the client implementation so realtime updates can import it.
export default StoresContentClient;
